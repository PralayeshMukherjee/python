from typing import List, Dict, Tuple, Union;

# list of integers 
number: List[int] = [1, 2, 3, 4, 5,];
# dictionary with string key and integer value
person: Dict[str, int] = {"age": 23};
# tuple with string and integer
coordinates: Tuple[str, int] = ("x",10);
#union typs for variables that can hold multiple types
identifier: Union[int, str] = "ID1233";
identifier = 2112;

# this are the inbuild types