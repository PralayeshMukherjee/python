# it is a immutable squence of datas denoted by parentheses
# it also can contain different types of data
a = (1,5,3,2);
print(type(a));

b = (1,) # it is a single element tuple
c = (1) # it is a integer, not a tuple

print(len(a)) # length of tuple
print(a[0]) # accessing first element of tuple